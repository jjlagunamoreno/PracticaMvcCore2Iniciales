@model IEnumerable<PracticaMvcCore2Iniciales.Models.Libro>

@{
    ViewData["Title"] = "Libros/Index";
}

<h1>Libros</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdLibro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Editorial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Portada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Resumen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGenero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdLibro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Autor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Editorial)
            </td>
            <td>
                    <img src="images/@Html.DisplayFor(modelItem => item.Portada)" style="width:100px; height:150px;" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Resumen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio) €
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGenero)
            </td>
                <td>
                    <a asp-controller="Libros"
                       asp-action="DetallesLibro"
                       asp-route-idlibro="@item.IdLibro"
                       class="btn btn-info">
                        Detalles
                    </a>
                    <a asp-controller="Libros"
                        asp-action="GuardarLibroCarrito"
                        asp-route-idLibro="@item.IdLibro"
                        class="btn btn-outline-primary">
                        Comprar
                    </a>
                </td>
        </tr>
}
    </tbody>
</table>
